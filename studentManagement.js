// Declare variables
let studentName;
let studentAge;
let studentGrade;
let isPresent;

// Assign sample values
studentName = "Jane";
studentAge = 16;
studentGrade = 11;
isPresent = true;

// Display in console
console.log(`Student Data for: ${studentName}`);
console.log(`- Age: ${studentAge}`);
console.log(`- Grade: ${studentGrade}`);
console.log(`- Is Present? ${isPresent ? "Present" : "Absent"}`);